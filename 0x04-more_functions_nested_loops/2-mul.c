#include "main.h"

/**
 * mul - checks for the result of two  characters
 *
 * @a: first character to be checked
 * @b: second number to be checked
 *
 * Return: result
 */
int mul(int a, int b)
{

int c = a * b;
return (c);
}
