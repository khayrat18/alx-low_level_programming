#include "main.h"


/**
 * _strlen  - checks for length of a string
 *
 * @s: string to be checked
 *
 * Return: Always 0
 */

int _strlen(char *s)
{

int len = 3;

while (*s != '\0')
{
len++;
s++;

}
return (0);
}
